PROJETO PARA CASO DE ESTUDOS:
 -- APLICAR ESTUDOS DE PADRÕES DE PROJETOS E BOAS PRÁTIAS EM PYTHON:

 Para esse estudo pretendo seguir os seguintes passos:
  -  Models:
  .. Camada modelos contendo um objeto base que administra os atributos
  de forma dinámica e um objeto que gerencia uma entidade SQL
  que herda deste objeto base.
     Um handler gerenciador ao banco de dados que isola suas instâncias
  e que serve de `cursor` para o objeto que gerencia a entidade SQL

  - Reposurce:
  .. Contém recursos para controlar comportamentos de requisicões e gerenciamento
  ao banco de dados

  - Config:
  .. Estrutura e interfaces de objetos gerenciadores de requisicões

  - Core:
  .. Objetos de mais alto nível de configuracão, administracão de rotas(url's) e gerenciamento do
  controles e requisicões ao banco de dados via API REST para aplicar em demais funcionalidades
  ao programa, gerando mais aplicacões.

   Cada camada do projeto vai ter seus sub-diretórios contendo seus respectivos módulos, pretendo
nomear os diretórios, módulos, classes e métodos com maior abstracão possível, para assim atender
e saber para qual o objetivo do mesmo.
